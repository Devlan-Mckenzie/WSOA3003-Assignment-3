The game and all the prototypes are built into 1 game(build).
The games are all played using the space bar as the input key.
The scene can be reloaded using the R key and they can be navigated using the Left and Right Arrow keys.
The escape key can be used to quit the game.